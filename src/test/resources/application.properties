# Cached Spring test contexts share one PostgreSQL instance. Bound each test pool;
# production pool sizing and runtime admission policy are unchanged.
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=0
