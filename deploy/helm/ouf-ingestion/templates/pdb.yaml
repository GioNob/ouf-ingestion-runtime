apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: {name: {{ include "ouf-ingestion.name" . }}}
spec:
  minAvailable: 1
  selector: {matchLabels: {app.kubernetes.io/name: {{ include "ouf-ingestion.name" . }}}}
