apiVersion: v1
kind: Service
metadata: {name: {{ include "ouf-ingestion.name" . }}}
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: {{ include "ouf-ingestion.name" . }}}
  ports: [{name: http, port: {{ .Values.service.port }}, targetPort: http, protocol: TCP}]
